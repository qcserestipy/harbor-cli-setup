name: "Harbor CLI Setup"
description: "Download, verify, and install the Harbor CLI"
author: "Patrick Eschenbach"

inputs:
  version:
    description: "Harbor CLI version to install, for example v0.0.1 or latest"
    required: false
    default: "latest"

  build_from_main:
    description: "Build Harbor CLI from the latest main branch instead of downloading a release"
    required: false
    default: "false"

runs:
  using: "node24"
  main: "dist/index.js"